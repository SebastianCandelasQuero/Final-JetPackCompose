package com.example.final_jetpackcompose.controller

class firebaseController {
    companion object {
        /*fun uploadCiudades() {
            val database = Firebase.database
            val ref = database.getReference("")

            val cities = listOf(
                "New York", "Los Angeles", "Chicago", "Houston", "London", "Paris",
                "Berlin", "Tokyo", "Beijing", "Sydney", "Moscow", "Istanbul",
                "Rio de Janeiro", "Cairo", "Mumbai", "Cape Town", "Toronto", "Dubai",
                "Mexico City", "Seoul", "Madrid", "Granada", "Sydney", "New Delhi", "Rome"
            )

            for (city in cities) {
                val cityRef = ref.child(city.replace(" ", "_"))
                val text = listOf<String>("prueba1", "prueba2", "prueba3")
                cityRef.setValue(text)
            }
        }*/
    }
}